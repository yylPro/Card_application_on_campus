# Application
PORT=4173
NODE_ENV=production
HTTPS=true
TLS_KEY_FILE=./certs/server-key.pem
TLS_CERT_FILE=./certs/server-cert.pem
TLS_PFX_FILE=
TLS_PFX_PASSWORD=
DATA_FILE=./data/db.json
DB_DRIVER=sqlserver
DB_HOST=localhost
DB_PORT=1433
DB_NAME=CampusService
DB_USER=
DB_PASSWORD=
DB_TRUST_CERT=true
TRUST_PROXY=false
ENABLE_PUBLIC_REGISTRATION=false

# Do not use the development defaults in production.
ADMIN_USER=operator
ADMIN_PASSWORD=AdminPass2026
# 可选：追加授权手机号的 SHA-256 摘要（不要填写手机号原文）
ADMIN_AUTHORIZED_PHONE_HASHES=
# 线下实体端授权手机号的 SHA-256 摘要（与运营商授权名单分开管理）
OFFLINE_AUTHORIZED_PHONE_HASHES=

# 32 字节随机密钥的 Base64 编码，用于加密已实名激活名单；配置后不可随意更换。
ACTIVATION_EXPORT_KEY=
# 32 字节随机密钥的 Base64 编码，用于加密身份证图片；配置后不可随意更换。
ID_IMAGE_ENCRYPTION_KEY=

# Optional external adapters. Leave empty until the corresponding service is authorized.
SMS_WEBHOOK_URL=
SMS_WEBHOOK_TOKEN=
SCHOOL_VERIFY_URL=
SCHOOL_VERIFY_TOKEN=

# Required only when enabling WeChat mini-program launch from the QR dispatch page.
WECHAT_OFFICIAL_APP_ID=
WECHAT_OFFICIAL_APP_SECRET=
WECHAT_MINIPROGRAM_APP_ID=
WECHAT_MINIPROGRAM_HOME=pages/home/home

# Non-production only. These numbers receive a developmentCode instead of real SMS.
TEST_PHONE_NUMBERS=13800000001,13800000002,13800000003,13900000001,13900000002
