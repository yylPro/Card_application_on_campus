# 校园通信服务正式版执行手册

文档版本：V1.0  
适用范围：微信小程序云开发部署、Node.js 服务器部署、运营商端、线下办理端和商家兑换端。

## 1. 发布边界

本项目提供两种可以独立运行的部署方式：

1. **微信云开发版**：使用 `miniprogram/` 和 `cloudfunctions/campusService/`，不需要服务器或域名。
2. **服务器版**：使用根目录的 `server.js`、H5 页面、`backend/` 和 `database/`，需要 Node.js、HTTPS 和数据库。

两种方式不要同时写入同一套业务数据。选择一种作为正式业务入口，另一种只用于迁移、备用或联调。

## 2. 部署文件

| 文件或目录 | 用途 |
| --- | --- |
| `miniprogram/` | 学生端、运营商端、线下办理端和商家兑换端 |
| `cloudfunctions/campusService/` | 云开发业务云函数 |
| `server.js`、`app.js`、`operator.js`、`offline.js` | 服务器版 API 和 H5 页面 |
| `backend/storage.js` | 服务器版 JSON、SQL Server、MySQL 存储适配 |
| `database/` | 服务器版数据库建表脚本 |
| `.env.example` | 服务器版配置模板，不包含生产密钥 |
| `scripts/manage-authorized-phone.js` | 服务器版授权手机号哈希管理 |

旧的 `内测执行手册.md` 已移除；本文件是正式部署和验收依据。

## 3. 微信云开发部署

### 3.1 小程序配置

1. 用微信开发者工具打开项目根目录。
2. 确认 `project.config.json` 的 `appid` 为正式小程序 AppID。
3. 确认 `miniprogram/app.js` 中的 `cloudEnvId` 为正式云环境 ID。
4. 上传并部署 `cloudfunctions/campusService`，选择“云端安装依赖”。
5. 部署后清除缓存并重新编译，检查首页、学校搜索和三个工作人员端入口。

### 3.2 云函数环境变量

云函数支持以下授权摘要配置，多个 SHA-256 摘要用英文逗号分隔。不要填写手机号原文：

- `CAMPUS_OPERATOR_PHONE_HASHES`：普通运营商账号。
- `CAMPUS_OPERATOR_BRANCH_PHONE_HASHES`：分公司运营商账号，可查看全部网格。
- `CAMPUS_OUTLET_PHONE_HASHES`：线下办理账号。
- `CAMPUS_MERCHANT_PHONE_HASHES`：商家兑换账号。

生成摘要示例：

```powershell
node -e "console.log(require('crypto').createHash('sha256').update('手机号').digest('hex'))"
```

云函数内置的开发授权摘要只用于已有授权人员的受控部署。正式环境应通过云函数环境变量维护名单，人员变更后重新部署并记录审批。

### 3.3 云数据库集合

首次调用云函数会尝试创建以下集合：

`campus_schools`、`campus_offers`、`campus_records`、`campus_accounts`、`campus_settings`。

云数据库权限应限制为云函数服务端读写。学生端不直接读写这些集合，所有业务请求通过 `campusService` 转发。

## 4. 服务器版部署

### 4.1 基础环境

- Node.js 18 或更高版本。
- 生产环境使用 SQL Server 或 MySQL，不使用 JSON 文件作为多人生产主库。
- 反向代理使用受信任 HTTPS 证书，只开放 443 端口。

```powershell
npm ci
npm test
npm run dev
```

生产环境可使用 PM2：

```powershell
pm2 start server.js --name campus-service
pm2 save
```

### 4.2 必要配置

从 `.env.example` 创建服务器私有 `.env`，至少配置：

```dotenv
NODE_ENV=production
DB_DRIVER=sqlserver
ADMIN_AUTHORIZED_PHONE_HASHES=
OFFLINE_AUTHORIZED_PHONE_HASHES=
ACTIVATION_EXPORT_KEY=
ID_IMAGE_ENCRYPTION_KEY=
```

`ACTIVATION_EXPORT_KEY` 和 `ID_IMAGE_ENCRYPTION_KEY` 必须是两把不同的 32 字节 Base64 密钥。密钥只保存于服务器私有配置和受控备份，不提交到 GitHub。

### 4.3 服务器版安全保存形式

服务器版保留旧代码的安全实现：

- 工作人员手机号只按 SHA-256 摘要匹配。
- 工作人员密码使用加盐摘要保存，不保存明文密码。
- 身份证图片使用 AES-256-GCM 加密后保存为 `.enc` 文件。
- 已激活名单归档使用独立 AES-256-GCM 密文，导出接口登录后才可访问。
- 数据库使用 `backend/storage.js` 适配 JSON、SQL Server 和 MySQL；正式多人环境选择一个主库。

部署包禁止包含 `.env`、数据库备份、身份证图片、导出 Excel、`data/db.json`、`.git` 和本地证书私钥。

## 5. 业务账号和权限

- 学生端：选择学校和二级学院、校园号码预约、本机查询订单和查看核销二维码。
- 普通网格运营商：只能查看、处理和导出所属网格订单。
- 青秀区分公司运营商：可选择网格，查看和导出对应网格订单。
- 线下办理端：输入校园号码和特征码，提交套卡实名已激活页面，完成实名核验。
- 商家兑换端：只能查询订单；订单通过线下核验后才能确认激活。

普通运营商和线下账号注册时必须填写网格；分公司账号由授权手机号识别，不受网格限制。

## 6. 正式验收流程

1. 学生选择指定学校、二级学院，提交校园号码预约。
2. 运营商确认订单进入列表，网格账号只能看到本网格订单，分公司账号可以切换网格。
3. 运营商设置统一线下实名地址并导出名单，检查创建时间和核验时间与订单一致。
4. 线下端输入校园号码，上传实名已激活页面，完成核验并导出当前账号的已核验名单。
5. 商家端查询同一校园号码，确认未核验订单不能激活，已核验订单可以确认激活。
6. 学生端重新查询，确认二维码显示“已核销”，订单状态显示“已完成”。
7. 运营商导出已激活和未激活两类名单，检查状态字段和实名人员、商家人员信息。

导出状态约定：

| 名单 | 交付状态 | 订单状态 | 实名状态 | 激活状态 |
| --- | --- | --- | --- | --- |
| 已激活 | 已交付 | 已完成 | 已实名 | 已激活 |
| 待激活 | 未交付 | 未完成 | 未实名 | 未激活 |

## 7. 备份、发布和回滚

发布前备份数据库、云数据库关键集合、服务器 `.env` 和两把加密密钥。先在备用环境恢复验证，再发布正式版本。

出现故障时：

1. 暂停新增订单和激活操作。
2. 保留错误时间、服务编码和云函数日志，不记录完整身份证号或密钥。
3. 回滚应用代码；不要直接删除订单或号码库存。
4. 确认数据库和号码状态一致后再恢复入口。

## 8. 上线检查

- 小程序云环境 ID 和云函数名称正确。
- 服务器 HTTPS、数据库连接和密钥配置正确。
- 授权手机号使用摘要，未把明文号码、密码或密钥提交到仓库。
- 云数据库和服务器数据库均完成备份恢复演练。
- 学校搜索、二级学院选择、订单查询、二维码核销、名单导出全部通过。
- `npm test` 通过，云函数依赖已在云端安装。
