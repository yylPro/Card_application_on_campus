const form = document.getElementById('adminRegisterForm'); const toast = document.getElementById('toast');
fetch('/api/auth/session').then((response) => response.json()).then((session) => { if (!session.registrationEnabled) location.replace('/admin/login'); }).catch(() => {});
function show(message, error = false) { toast.textContent = message; toast.classList.toggle('error', error); toast.classList.add('show'); setTimeout(() => toast.classList.remove('show'), 3200); }
form.addEventListener('submit', async (event) => { event.preventDefault(); const button = form.querySelector('button'); button.disabled = true; try { const response = await fetch('/api/auth/register', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(form))) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || '注册失败'); location.replace('/admin'); } catch (error) { show(error.message || '网络连接失败，请稍后重试', true); } finally { button.disabled = false; } });
