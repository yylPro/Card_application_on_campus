# 校园通信选号与线下实名激活系统内测执行手册

文档版本：V0.2
适用范围：学生端、运营商端、线下实体端的封闭内测及公网内测前验收
测试结论记录人：________  测试日期：________  测试环境：________

## 1. 当前发布结论

**当前版本可以在受控内网或本机继续功能内测，但禁止直接暴露到公网。** 本机 `.env` 已于 2026-08-11 配置两把相互独立的 32 字节内测密钥，密钥值不得记录在本文档、截图或问题单中。

公网开放前必须关闭以下 P0 风险：

| 编号 | 当前风险 | 公网前验收标准 |
| --- | --- | --- |
| P0-1 | 代码已改为静态资源白名单，仍需部署后复核 | 上述路径及 `data/`、`certs/`、`database/`、`backend/`、`test/` 一律返回 404 |
| P0-2 | 生产环境默认关闭三端自助注册；如重新开启，仍需验证手机号所有权 | 公网内测使用预创建账号，或先接入短信验证码 |
| P0-3 | 新上传身份证图片已使用 AES-256-GCM 加密落盘；截至 2026-08-11 的历史测试明文图片已删除 | 上线前复核上传目录只含 `.enc` 文件，并确认上传目录不能由静态服务直接读取 |
| P0-4 | 本机已配置内测用 `ACTIVATION_EXPORT_KEY` 和 `ID_IMAGE_ENCRYPTION_KEY`，不得直接沿用到正式环境 | 清空内测敏感数据，在正式服务器重新生成两把不同密钥，完成独立备份，并验证服务重启后仍能读取新密文和导出归档 |
| P0-5 | 当前配置为 `NODE_ENV=development` | 改为 `NODE_ENV=production`，确认测试接口关闭、默认配置失效且服务能正常启动 |
| P0-6 | 生产代码已移除内置运营商和实体端测试特权号码 | 只从服务器私密配置读取正式授权手机号摘要，并完成双人复核 |

任一 P0 项未通过时，不得开放公网端口，也不得录入真实身份证、真实身份证图片或真实业务号码。

### 内网测试边界

- 只允许测试设备所在的受控局域网访问，使用防火墙限制来源网段，不做公网端口映射、临时穿透或路由器 DMZ 暴露。
- 跨设备测试必须使用 HTTPS。HTTP 预览只允许本机检查页面，禁止通过 HTTP 提交身份证号、证件图片、密码或真实手机号。
- 当前为 `NODE_ENV=development`，开发测试号码和测试入口仍然存在，只能使用虚构学生资料、合成身份证测试值和带“仅供系统内测”水印的测试图片。
- 内网测试结束后按第 9 节清理订单、账号、证件图片、导出文件和日志；确认无需保留内测密文后，再于正式上线前轮换两把密钥。

### 已完成的代码级加固

- 静态资源采用白名单，工作区中的配置、源码、数据库、证书、测试目录和上传目录不对外提供。
- 身份证图片使用 AES-256-GCM 加密后以 `.enc` 文件保存，不再保存明文 JPG/PNG。
- 身份证号校验行政区前缀、出生日期、顺序码和 GB 11643 校验位；图片校验 PNG/JPEG 签名、结构、MIME、尺寸和大小。
- 已实名激活名单使用独立 AES-256-GCM 密文归档，运营商登录后才能导出 Excel。
- 生产环境默认关闭三端自助注册，生产环境禁止 JSON 数据库，缺少两类加密密钥时拒绝启动。
- 统一补充 CSP、HSTS、`X-Frame-Options`、`Referrer-Policy` 和 `Permissions-Policy` 响应头。

## 2. 三端入口

部署后把下列 `{测试域名}` 替换为实际 HTTPS 域名：

| 角色 | 登录/入口 | 权限说明 |
| --- | --- | --- |
| 学生端 | `https://{测试域名}/student/login` | 注册、选号、提交订单、查询线下地址和本人特征码、查看激活状态 |
| 运营商端 | `https://{测试域名}/admin/login` | 导入号码、设置全局线下实名地址、处理订单、导出已激活名单 |
| 线下实体端 | `https://{测试域名}/offline/login` | 授权工作人员匹配学生身份与选号号码，并确认实名激活 |
| 健康检查 | `https://{测试域名}/api/health` | 仅用于确认应用存活，不应返回配置或个人信息 |

公网只开放 HTTPS 443。应用端口、SQL Server/MySQL 端口及管理端口不得直接暴露公网。

## 3. 内测数据

### 3.1 测试人员号码

公网内测必须使用测试人员本人可控制、可接收验证码的真实手机号。人员名单和手机号由测试负责人线下发放，不写入 Git、群公告或本文档。

以下号码仅用于本机自动化或隔离环境，不得接入真实短信网关，不得视为公网账号：

| 用途 | 本地测试号码 |
| --- | --- |
| 学生流程 | `13800000001`、`13800000002`、`13800000003` |
| 并发选号 | `13900000001`、`13900000002` |
| 线下实体端开发账号 | `18500000001` |
| 运营商端开发账号 | `13987654321`、`18600000001` |

密码由每位测试人员单独设置，必须为 9-15 位并同时包含大写字母、小写字母和数字。禁止所有角色共用密码，禁止把密码写入本文档。

### 3.2 测试号码池

可使用下列脱敏号码验证筛选、预占和激活状态。它们是界面测试资源，不代表真实可开卡号码：

| 运营商 | 测试选号号码 | 套餐名称 | 建议月费 |
| --- | --- | --- | --- |
| 中国移动 | `138****0001` | 校园号码套餐 A | 0 元 |
| 中国移动 | `139****0002` | 校园号码套餐 B | 0 元 |
| 中国移动 | `158****0003` | 校园号码套餐 C | 0 元 |
| 中国联通 | `186****0001` | 校园号码套餐 A | 0 元 |
| 中国联通 | `185****0002` | 校园号码套餐 B | 0 元 |
| 中国联通 | `130****0003` | 校园号码套餐 C | 0 元 |
| 中国电信 | `189****0001` | 校园号码套餐 A | 0 元 |
| 中国电信 | `181****0002` | 校园号码套餐 B | 0 元 |
| 中国电信 | `133****0003` | 校园号码套餐 C | 0 元 |

生产模式不会自动加入开发测试号码。公网内测前需由运营商端把测试号码导入指定学校，Excel 首行使用：`运营商、可选号码、套餐名称、月费`。

### 3.3 学生资料

功能测试统一使用虚构资料，例如：

| 字段 | 示例 |
| --- | --- |
| 姓名 | 内测学生甲、内测学生乙 |
| 学号 | TEST20260001、TEST20260002 |
| 身份证格式测试值 | `11010119900101004X`、`110101199001010058`（均为合成测试值） |
| 地址 | 内测宿舍 1 栋 101 |
| 需求说明 | 公网内测选号流程 |

身份证图片必须使用明确加水印的测试图片，水印内容建议为“仅供系统内测，非真实证件”。P0 安全项未完成前严禁上传真实身份证图片。

系统只能判断身份证号结构和校验位是否合理、上传内容是否为可识别图片，不能判断号码是否真实签发或图片是否为真实证件。线下激活必须由工作人员核对学生本人和身份证原件；需要线上自动判真时，应接入有合法授权的权威身份核验服务，不能用前端校验或普通图片识别替代。

## 4. 公网内测前准备

1. 使用独立测试域名和受信任 CA 签发的 TLS 证书，不使用自签名证书。
2. 设置 `NODE_ENV=production`、`HTTPS=true`，并确认实际访问强制跳转 HTTPS。
3. 使用 SQL Server 或 MySQL，禁止公网多人测试使用 JSON 文件存储。
4. 配置最小权限数据库账号；数据库只允许应用服务器内网地址连接。
5. 设置独立的 `ADMIN_AUTHORIZED_PHONE_HASHES` 和 `OFFLINE_AUTHORIZED_PHONE_HASHES`。
6. 在正式服务器重新生成并备份 `ACTIVATION_EXPORT_KEY` 和 `ID_IMAGE_ENCRYPTION_KEY`，两把密钥不得相同，也不得写入 Git、文档、聊天记录或数据库。
7. 修复静态文件泄露后，验证 `/.env`、`/server.js`、`/data/db.json`、`/data/id-images/任意文件` 均返回 404。
8. 接入短信验证码，或预先创建测试账号并关闭三端注册能力。
9. 反向代理必须覆盖客户端传入的 `X-Forwarded-For`，否则登录限流可能被伪造请求头绕过。
10. 配置 HSTS、CSP、`X-Frame-Options`/`frame-ancestors`、`Referrer-Policy` 和 `Permissions-Policy`。
11. 对数据库、身份证图片和加密密钥分别备份，并执行一次实际恢复演练。
12. 执行 `npm ci`、`npm test`，要求全部测试通过后再部署。

### 4.1 上传文件边界

建议在干净目录中从 Git 版本构建，不要直接把当前工作目录整体复制到公网服务器。

应部署：业务源码、`backend/`、数据库结构脚本、前端 HTML/JS/CSS、`miniprogram/`、`package.json`、`package-lock.json`，以及经过负责人确认不含个人信息的学校基础资料。

禁止上传：

- `.env` 和任何包含真实密码、手机号白名单摘要、API 令牌或加密密钥的文件。
- `.git/`、`node_modules/`、本地日志、测试输出和临时文件。
- `data/db.json`、`data/id-images/`、历史 Excel 导出和数据库备份。
- `certs/` 中的本地自签名证书与私钥。
- 未脱敏的截图、真实身份证图片和测试人员名单。

服务器上的 `.env`、TLS 私钥和两类加密密钥必须通过独立安全渠道创建，不能随部署包上传。可在服务器管理终端运行两次以下命令分别生成密钥，生成结果只能写入服务器私密配置：

```powershell
node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
```

分别填写 `ACTIVATION_EXPORT_KEY` 和 `ID_IMAGE_ENCRYPTION_KEY`，两者不得相同。配置完成后备份到受控的密码管理系统。

## 5. 主流程测试

### 5.1 运营商准备

1. 使用运营商授权手机号注册并登录运营商端。
2. 导入至少 9 个测试号码，确认移动、联通、电信各有 3 个可选号码。
3. 在“线下实名认证地址”中填写全局地址，例如“东校区大学生活动中心一楼运营商服务点”。
4. 保存后刷新页面，确认地址仍然存在。

预期结果：全局地址保存成功；该地址适用于全部尚未激活及后续新增的选号订单。

### 5.2 学生选号并提交

1. 使用测试学生手机号注册并登录学生端。
2. 搜索并选择学校、学院，填写虚构学生资料。
3. 上传带“仅供内测”水印的正反面测试图片。
4. 进入选号页面，分别检查三个运营商标签、号码搜索和分页。
5. 选择一个测试号码，填写完整收货地址后提交选号订单。

预期结果：

- 订单创建成功并生成服务编号。
- 运营商端订单详情显示学生填写的收货地址，并与全局线下实名认证地址明确区分。
- 号码从可选池中消失，状态变为已预占。
- 实名状态为“待线下实名”。
- 激活状态为“待激活”。
- 学生不能直接把号码改成已激活。

### 5.3 学生查询线下信息

1. 学生打开“查询进度”。
2. 使用本人账号查询刚才的选号订单。
3. 记录页面显示的线下实名地址和 8 位特征码。

预期结果：地址与运营商设置的全局地址一致；特征码只属于该学生订单；页面仍显示“待激活”。

### 5.4 线下实体匹配

1. 实体端工作人员使用已授权手机号和本人密码登录。
2. 输入学生出示的特征码和学生身份证号码。
3. 点击“匹配学生与选号号码”。
4. 核对页面显示的姓名、原联系电话、备用联系电话、学校、运营商和选号号码。

预期结果：正确的“特征码 + 身份证号”只返回一个学生和一个选号号码；此时尚未激活。

### 5.5 确认实名并激活

1. 工作人员核对学生本人及身份证原件。
2. 点击“确认实名并激活”。
3. 不重复点击，不刷新提交中的页面。

预期结果：

- 实体端显示号码已激活。
- 学生端查询显示“号码已实名激活”。
- 运营商端显示实名已核验、号码已激活、订单已完成。
- 号码资源状态为已激活，不能再次被选择。
- 相同特征码不能重复激活。

### 5.6 导出已激活名单

1. 运营商登录后台，点击“导出已激活”。
2. 打开 Excel，检查工作表“已实名激活名单”。
3. 核对列顺序：学校、学院、姓名、身份证号、选号号码、联系电话、备选联系电话、激活状态。
4. 核对本次学生记录只出现一次，激活状态为“已激活”。

预期结果：未登录访问导出接口返回 401；数据库归档只包含 AES-256-GCM 密文、IV 和认证标签，不出现上述名单的明文字段。

## 6. 必测异常场景

| 场景 | 操作 | 预期结果 |
| --- | --- | --- |
| 错误特征码 | 正确身份证号配错误特征码 | 匹配失败，不展示学生资料，不修改状态 |
| 错误身份证号 | 正确特征码配错误身份证号 | 匹配失败，不展示学生资料，不修改状态 |
| 错误身份证校验位 | 提交末位校验码错误的 18 位号码 | 提交失败，提示核对身份证号 |
| 伪装图片 | 将非图片内容声明为 PNG/JPEG 上传 | 上传失败，不生成落盘文件 |
| 过小图片 | 上传小于 300×180 的图片 | 上传失败，提示图片尺寸不足 |
| 重复激活 | 对已激活订单再次匹配 | 拒绝操作，不生成重复归档 |
| 未授权实体账号 | 使用不在白名单的手机号注册 | 返回未授权 |
| 运营商越权激活 | 在运营商端尝试手工修改激活状态 | 拒绝，激活只能由实体端确认 |
| 同号并发 | 两名学生同时选择同一号码 | 只允许一人成功，另一人收到号码已预占提示 |
| 取消订单 | 运营商取消未激活选号订单 | 号码回到可选池，原特征码不能激活 |
| 修改全局地址 | 有多个待激活订单时修改地址 | 所有待激活订单地址同步更新，个人特征码不变 |
| 已激活后改地址 | 修改全局地址 | 已激活记录保留原办理信息和状态 |
| 未登录导出 | 直接访问已激活名单接口 | 返回 401，不下载文件 |
| 密钥不一致 | 在隔离备份环境使用错误密钥导出 | 导出失败且不返回部分明文数据 |
| 超大文件 | 上传超过限制的图片或 Excel | 请求被拒绝，服务保持可用 |
| 登录暴力尝试 | 连续输入错误密码 | 触发限流，不泄露账号是否存在 |

## 7. 安全检查记录

| 检查项 | 结果 | 证据/备注 |
| --- | --- | --- |
| 所有入口均为受信任 HTTPS | 通过 / 不通过 | |
| HTTP 自动跳转 HTTPS | 通过 / 不通过 | |
| `.env`、源码、数据库和身份证目录不可公网访问 | 通过 / 不通过 | |
| 特权账号注册已验证手机号所有权或已关闭 | 通过 / 不通过 | |
| 生产环境无内置测试特权号码 | 通过 / 不通过 | |
| 数据库使用最小权限账号 | 通过 / 不通过 | |
| 身份证图片已加密且不可静态访问 | 通过 / 不通过 | |
| 激活名单归档为 AES-256-GCM 密文 | 通过 / 不通过 | |
| 加密密钥已独立备份并完成恢复验证 | 通过 / 不通过 | |
| Excel 导出只对运营商授权账号开放 | 通过 / 不通过 | |
| 日志不记录密码、密钥、完整身份证图片内容 | 通过 / 不通过 | |
| 数据库、图片、密钥备份可恢复 | 通过 / 不通过 | |

## 8. 问题记录模板

每个问题至少记录以下内容：

- 问题编号：
- 发现时间：
- 测试角色：学生端 / 运营商端 / 实体端
- 使用浏览器及版本：
- 服务编号：仅填写测试服务编号，不在群内发送身份证号
- 前置条件：
- 操作步骤：
- 实际结果：
- 预期结果：
- 截图或日志位置：敏感信息必须打码
- 严重程度：阻断 / 严重 / 一般 / 建议
- 处理人及状态：

## 9. 内测结束处理

1. 关闭公网入口或撤销测试域名解析。
2. 停用所有临时运营商和实体端授权手机号。
3. 删除测试身份证图片、测试订单、测试账号和测试 Excel。
4. 检查备份中是否包含测试个人信息，并按留存策略清理。
5. 轮换测试期间使用的账号密码、数据库密码和 API 令牌。
6. 本轮使用的是内测密钥：确认内测敏感数据和密文已清空后，正式上线前重新生成两把生产密钥；如需保留历史密文，必须先完成迁移和解密验证，不能直接换钥。
7. 汇总主流程通过率、异常场景结果和未关闭问题，再决定是否进入下一轮。

## 10. 放行标准

满足以下全部条件才能开始公网内测：

- P0 风险全部关闭并由第二人复核。
- 自动化测试全部通过。
- 主流程至少由两组不同测试人员完整走通。
- 三家运营商测试号码都完成一次选号、实名、激活和导出。
- 错误身份证、错误特征码、未授权登录、重复激活和并发抢号均按预期失败。
- 安全检查记录全部通过，没有未关闭的阻断或严重问题。
