const { request } = require('../../utils/api');
const app = getApp();
Page({ data: { recordId: '', phone: '', rating: 5, ratingComment: '', ratings: ['5 星：非常满意', '4 星：满意', '3 星：一般', '2 星：不满意', '1 星：非常不满意'], selectedRating: '5 星：非常满意' }, onLoad(query) { this.setData({ recordId: query.id || '', phone: query.phone || '' }); }, setComment(event) { this.setData({ ratingComment: event.detail.value }); }, pickRating(event) { const index = Number(event.detail.value); this.setData({ rating: 5 - index, selectedRating: this.data.ratings[index] }); }, async submit() { try { await request('/api/student/confirm-completion', 'POST', { schoolCode: app.globalData.schoolCode, phone: this.data.phone, recordId: this.data.recordId, rating: this.data.rating, ratingComment: this.data.ratingComment }); wx.showToast({ title: '感谢您的评价', icon: 'success' }); setTimeout(() => wx.navigateBack(), 800); } catch (error) { wx.showToast({ title: error.message, icon: 'none' }); } } });
